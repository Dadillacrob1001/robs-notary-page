# Mock Notary Booking Page

A simple real-time appointment booking demo using HTML, JavaScript, Express, and Socket.IO.

## Quick start

1. Run `npm install`
2. Run `npm start`
3. Open `http://localhost:3000`

## Features

- Home page with navbar
- Calendar date picker and available time slots
- Real-time updates across connected clients
- Simple booking form
